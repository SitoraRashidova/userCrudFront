import React from "react";
import { useDispatch } from "react-redux";
const Hero = () => {
  return (
    <div>
      <h2>Hero</h2>
      <button>Increment</button>
    </div>
  );
};

export default Hero;
